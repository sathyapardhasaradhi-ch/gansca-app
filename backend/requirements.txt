# --- Original inference stack (from the provided backend) ---
numpy>=1.24.0
rasterio>=1.3.0
torch>=2.0.0
rich>=13.0.0

# --- Web application layer ---
Flask>=3.0.0
pyproj>=3.5.0      # reproject building vectors to WGS84 for web maps
Pillow>=10.0.0     # render a browser-viewable PNG preview of the mask
